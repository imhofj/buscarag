[pytest]
# Permite que los tests importen ingest.py y rag.py desde la raíz del proyecto
pythonpath = .
testpaths = tests
# Ocultar avisos de librerías externas que no tienen que ver con el proyecto
filterwarnings =
    ignore::DeprecationWarning
